runtime: nodejs24

instance_class: F1

automatic_scaling:
  min_instances: 0
  max_instances: 5
  target_cpu_utilization: 0.65
  max_concurrent_requests: 10

env_variables:
  NODE_ENV: 'production'
  SECRETS_PROJECTS_ID: 'lyrical-amulet-495920-u8' # add the project ID hosting the secrets here
  DATABASE_NAME: 'mongo-database'